# Apollo (Layer 2 — enrollment & enrichment)
APOLLO_API_KEY=
# Anthropic (Layer 5 — reply triage)
ANTHROPIC_API_KEY=
# Slack (Layer 4 — monitoring alerts)
SLACK_WEBHOOK_URL=
# Instantly (Layer 5 — reply polling)
INSTANTLY_API_KEY=
# GitHub (Layer 1 — higher signal rate limit; optional)
GITHUB_TOKEN=
# Vercel Postgres — auto-injected by Vercel; for local dev paste the POSTGRES_* vars
POSTGRES_URL=
# Protect the cron + db-init endpoints
CRON_SECRET=

# --- Google login (Auth.js) — app stays public until all three are set ---
AUTH_SECRET=
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=
